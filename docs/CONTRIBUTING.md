# Contributing Guide

## Standards
- TypeScript strictness and modular architecture
- Small, isolated PRs
- Tests required for platform detection and critical sync logic

## Workflow
1. Fork branch
2. Implement change
3. Run `npm run lint && npm run test && npm run build`
4. Open PR with clear summary and screenshots for UI changes
